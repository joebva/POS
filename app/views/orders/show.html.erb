<p id="notice"><%= notice %></p>

<fieldset>
  <legend>Customer Info</legend>
<p><b>Customer:</b></p>
<p><b>Phone:</b></p>
<p><b>Address:</b></p>


<%= link_to 'Edit Order', edit_order_path(@order) %>

</fieldset>

<fieldset>
  <legend>Order #<%= @order.id %></legend>
<table>
  <tr>
    <th>Menu Item ID</th>
    <th>Menu Item Name</th>
  </tr>

<% @order.lineitems.each do |lineitem| %>
  <tr>
    <td><%= lineitem.menuitem_id %></td>
    <td><%= lineitem.menuitem.name %></td>
  </tr>
<% end %>
</table>

</fieldset>


<fieldset>
  <legend>Cart #<%= @cart.id %></legend>
  <table>
  <tr>
    <th>menuitemID</th>
    <th>Name</th>
  </tr>

  <% @cart.lineitems.each do |cartitem| %>
  <tr>
    <td><%= cartitem.menuitem_id %></td>
    <td><%= cartitem.menuitem.name %></td>
  </tr>
  <% end %>
  </table>

<%= link_to "Add Cart to Order", {:action => :update, :id => @order.id}, :method=> :put %>
<% #link_to 'Empty cart', @cart, method: :delete, data: { confirm: 'Are you sure?' } %> 
<%= link_to 'Empty Cart 2', cart_path(@cart, :order_id=> @order.id), :method=> :delete %>
</fieldset>


<%= link_to 'All Orders', orders_path %>

</BR></BR>
<fieldset>
  <legend><B>Menu Items</B></legend>
<table>
  <tr>
    <th>Name</th>
    <th>Price</th>
    <th></th>
  </tr>

<% @menuitems.each do |menuitem| %>
  <tr>
    <td><%= menuitem.name %></td>
    <td><%= menuitem.price %></td>
    <td><%= link_to 'Add to Cart', lineitems_path(menuitem_id: menuitem, :order_id => @order.id), :method=> :post %></td>
    <td><%= link_to 'Description', menuitem %></td>
  </tr>
<% end %>
</table>
</fieldset>
